<UserControl x:Class="WpfChat.View.ChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfChat.View"
        mc:Ignorable="d"
        Height="700" Width="485">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding Messages}" Grid.Row="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Right"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Text}"
                           Margin="5" 
                           Background="LightGray"
                           Padding="10"
                           TextWrapping="Wrap"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" Grid.Row="1" >
            <TextBox Text="{Binding NewMessage, UpdateSourceTrigger=PropertyChanged}" Width="443" Height="35" TextWrapping="Wrap" KeyDown="TextBox_KeyDown"/>
            <Button Content="보내기" Command="{Binding SendMessageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
