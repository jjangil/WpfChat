<UserControl x:Class="WpfChat.View.FriendView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfChat.View"
        mc:Ignorable="d"
        Height="800" Width="500">
    <Grid>
        <ListBox ItemsSource="{Binding Friends}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <!-- Ellipse는 WPF에서 사용되는 도형 컨트롤-->
                        <Ellipse Width="40" Height="40" Fill="LightGray" Margin="0,0,10,0"/>
                        <!-- 이름 + 상태메시지 -->
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
